# JSON Output Contract

Respond with a single JSON object only. No Markdown fences, no prose before or after.

Use double-quoted keys and strings. Omit null fields when possible.

If a schema hint is provided, match it exactly.
