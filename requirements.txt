# maads scaffold — direct runtime dependencies
python-dotenv>=1.0
openai>=1.0
tiktoken>=0.5
pydantic>=2.0
PyYAML>=6.0

# CrewAI (>=1.10.1 required for openinference auto-instrumentation)
crewai>=1.10.1,<2.0
crewai-tools>=1.10.1,<2.0

# ML stack — Phase 1 fixed-baseline modeling (canonical list lives in pyproject.toml)
pandas>=2.1
scikit-learn>=1.4
pyarrow>=15.0
